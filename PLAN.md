# PLAN — Paper horse-race → edge probado

> Norte: encontrar un **edge real en paper** (estrategia que le gane al mercado, no solo a las otras), con riesgo y disciplina por encima de todo. NUNCA dinero real sin edge probado. Rama: `feature/paper-horse-race` (NO mergeada).
> Estado vivo: **4 caballos × $25k = $100k** sobre 1 cuenta Alpaca paper, corriendo L-V 13:00 CST vía LaunchAgent `com.luisfer.horserace`. **RESET por Luis el 2026-06-25** (`scripts/reset_race.py`, commits be08ffb + 0eb2dc6): añadió 4º caballo `donchian_breakout` (20/10 ruptura), aplicó sector-cap a momentum + filtro de tendencia per-name a confirmed_mr, flatteó la cuenta paper y rotó la curva. **Inception nueva = 2026-06-25**; la curva 3-caballos vieja (06-05→06-23) quedó archivada en `data/ledgers/*.bak-20260625-091940`. La ventana de alpha del experimento previo (06-16→06-30) quedó CANCELADA — empieza ventana nueva con 4 caballos.

## Prioridad

1. **[EN CURSO] Medir edge contra benchmark, no solo entre caballos.**
   - [x] Benchmark SPY buy&hold + columna `alpha%` en el reporte (`live/benchmark.py`, anclado al 2026-06-05). Hecho 2026-06-14, 63/63 tests verde.
   - [x] **BUG CRÍTICO arreglado 2026-06-15:** el alpha NUNCA se calculaba en prod. SPY no es constituyente del S&P 500 → no estaba en el universo descargado → `compute_benchmark` devolvía `None` en cada corrida → `alpha_pct: null` en toda la curva y la columna `alpha%` se caía del reporte. Los tests pasaban porque inyectaban SPY en el fixture (test-verde/prod-roto). Fix: `FETCH_SYMBOLS = UNIVERSE + [SPY]` en `run_trading_system.py` (SPY se descarga pero el orchestrator solo pide símbolos de estrategia, así que NUNCA se opera). Regresión cerrada con `tests/test_benchmark_wiring.py` (5 tests). Verificado end-to-end: SPY +2.35% desde 2026-06-05. **El alpha real empieza a registrarse en la próxima corrida (mar 2026-06-16).**
   - [x] Lectura de checkpoint on-demand: `live/checkpoint_report.py` (+ `tests/test_checkpoint_report.py`, 13 tests) consolida por caballo equity/return/alpha + max-DD/vol + un veredicto descriptivo en UNA tabla, leyendo solo la curva persistida (sin red, sin Alpaca). Correr: `.venv-bt/bin/python -m live.checkpoint_report`. NO automatiza ninguna decisión.
   - [x] **Verificación end-to-end del fix de alpha (2026-06-15, loop):** confirmado que el path de prod produce benchmark SPY +2.34% (no null) → el alpha real arranca bien en la corrida del 2026-06-16. De paso encontré y arreglé un footgun latente: `get_bars_batch` descartaba en silencio un fetch de UN solo símbolo (yfinance devuelve columnas MultiIndex aun para 1 ticker; el branch `else raw` se las pasaba crudas a `_normalize` y volvían vacías). Misma clase test-verde/prod-roto que el bug de SPY-fuera-del-universo. Arreglado + 2 tests de regresión. Prod (503 símbolos) nunca lo pegó, pero un refresh benchmark-only de SPY sí. 97/97 verde. Commit `dcc6259`.
   - [x] **Guard anti-regresión silenciosa (2026-06-15, loop):** el bug de alpha-null vivió 10 días sin un solo aviso en el log. Extraído `resolve_benchmark()` en `run_trading_system.py` que loguea un `WARNING` a `cron.log` cuando el benchmark sale None, distinguiendo "SPY nunca se fetcheó" de "se fetcheó pero sin barra at/after inception". Ahora la clase de bug se caza en cada corrida, no al cerrar la ventana de 2 semanas. Commit `02343d3`, 4 tests nuevos (`tests/test_resolve_benchmark.py`), 101/101 verde. (El snapshot del 2026-06-15 sigue en null porque el fix `13afb6e` aterrizó 13:06, después de la corrida de 13:00 — el alpha real arranca el 2026-06-16.)
   - [x] **Detección de huecos en la curva (2026-06-16, loop):** misma clase test-verde/prod-roto. `vol`/`max_dd` asumen que dos snapshots consecutivos son días de trading consecutivos; si una corrida del cron se salta un día (justo lo que vigila el item #4), la curva queda con hueco y la volatilidad se infla en silencio + una curva rala podría pasar el veredicto `≥5 días`. `risk_metrics` ahora cuenta `gap_days` (días hábiles L-V faltantes, fines de semana ignorados) por caballo; `checkpoint_report` imprime un aviso `⚠ GAP` ruidoso cuando algún caballo tiene curva no contigua. Descriptivo, no automatiza nada. Commit en `feature/paper-horse-race`, 9 tests nuevos, 110/110 verde.
   - [x] **Veredicto honesto: alpha-días reales + estabilidad, no equity-días (2026-06-16, loop):** misma clase test-verde/prod-roto, pero en el componente que alimenta la decisión IRREVERSIBLE de dinero (`classify_edge` en `checkpoint_report`). El veredicto (a) gateaba en `n_days` = días de equity, pero el alpha fue null hasta 06-16 → la curva llega a 5 equity-días antes que a 5 alpha-días reales, y habría declarado "edge candidate" con 1-2 observaciones de alpha mientras mostraba "5 days"; y (b) leía solo el alpha del ÚLTIMO snapshot, no estabilidad — un rebote negativo→positivo el último día pasaba como edge, violando el "positivo **y estable**" del PLAN. Fix: `classify_edge` ahora recibe la **serie** de alpha cronológica, gatea en observaciones reales (no-null) y marca `edge? but unstable (alpha dipped ≤0)` si el alpha tocó ≤0 dentro de la ventana. Nueva nota `ℹ alpha lag` en el readout explica por qué la columna "days" puede verse larga y el veredicto seguir inconclusive. Verificado en prod: los 3 caballos leen `inconclusive (need ≥5 alpha days)` con 2 alpha-días reales. Descriptivo, no automatiza nada. Commit en `feature/paper-horse-race`, 9 tests reescritos/nuevos, 115/115 verde.
   - [x] **HITO confirmado en prod (2026-06-16, loop):** la corrida de las 13:00 CST registró por PRIMERA VEZ alpha real (no-null) en `equity_curve.jsonl`: momentum_rotation +9.20%, rsi_mr +2.29%, confirmed_mr −4.10% vs SPY +1.90%. Cierra los 10 días de bug alpha-null en producción; el log muestra `benchmark(SPY)=True`, exit 0. La ventana útil de alpha corre ahora ~2026-06-16 → 2026-06-30.
   - [x] **Ventana de riesgo divulgada (2026-06-16, loop):** misma clase test-verde/prod-roto, en el componente que alimenta la decisión IRREVERSIBLE. `return%`/`equity` son acumulados desde inception (2026-06-05), pero `max_dd%`/`vol%` solo cubren la curva persistida, que empezó a acumular el 2026-06-15 (~10 días tarde — `equity_snapshot.py` se cableó el 06-14). Un lector del checkpoint leería `max_dd −2.09%` como "peor DD desde inception" y se perdería cualquier drawdown anterior al primer snapshot. `checkpoint_report` ahora adjunta `curve_start`/`curve_end` por caballo e imprime una nota `ℹ risk window` que dice explícitamente que max_dd es worst-since-curve-start, NO worst-since-inception. Descriptivo, no automatiza nada. Commit `52d75c9`, 3 tests nuevos, 118/118 verde.
   - [x] **Columna alpha-days en el readout (2026-06-16, loop):** misma clase readout-engaña en el componente que alimenta la decisión IRREVERSIBLE. La tabla mostraba `days` = largo de la curva de equity, pero el veredicto gatea en **alpha-days** (observaciones de alpha no-null). Mientras el alpha fue null pre-benchmark divergen: prod lee `days=2` / `alpha-days=1`, pero la tabla solo mostraba 2 junto a `need ≥5 alpha days` — un lector no podía ver que solo tenemos 1 alpha-día. La nota `ℹ alpha lag` que lo explicaría sigue suprimida hasta que los equity-días solos lleguen a 5, así que la columna es la única señal honesta en el caso cotidiano. Añadida columna `αdays`. Descriptivo, no automatiza nada. Commit `7ce815a`, 1 test nuevo, 119/119 verde.
   - [x] **Gate señal-vs-ruido en el veredicto (2026-06-17, loop):** el componente que alimenta la decisión IRREVERSIBLE declaraba "edge candidate" con alpha positivo y estable, pero SIN noción de magnitud-vs-ruido. Con 3 caballos × ~10 días, que uno le gane a SPY por suerte es EL riesgo estadístico central: un alpha de +0.1% saltarín leía idéntico a uno de +9% sólido. `classify_edge` ahora computa un info-ratio crudo (media del alpha / su stdev diaria); si la media no supera su propia dispersión (`ratio < 1`) marca `edge? but within noise (alpha < its own swing)` en vez de candidate. Stdev 0 (alpha plano positivo) o <2 obs → no penaliza (un alpha consistente y plano es MÁS convincente, no menos). Disciplina sobre retorno, descriptivo, no automatiza nada. Commit en `feature/paper-horse-race`, 2 tests nuevos, 121/121 verde.
   - [x] **Aviso de sesgo de selección en el readout (2026-06-17, loop):** el PLAN nombra como riesgo estadístico CENTRAL que, con 3 caballos × ~10 días, el mejor le gane a SPY por suerte. `classify_edge` gatea cada caballo contra su PROPIO ruido (info-ratio), pero no ve que el operador leerá al **mejor de N** — y la probabilidad de que *algún* caballo pase el filtro por azar escala con N. Añadida nota `⚠ selection bias` en `checkpoint_report`: cuando ≥2 caballos tienen veredicto real y ≥1 lee `edge candidate`, avisa que el edge del ganador está sesgado al alza y que debe tratarse como *promising, NOT proven* (exigir persistencia/out-of-sample antes de contar para la decisión de dinero). Dormida hasta que emerja un candidato real (hoy los 3 leen `inconclusive`, así que no aparece todavía). Descriptiva, no automatiza nada. Commit en `feature/paper-horse-race`, 3 tests nuevos, 124/124 verde.
   - [x] **Info-ratio sobre el active return diario, no sobre niveles acumulados (2026-06-18, loop):** misma clase test-verde/prod-roto en el componente que alimenta la decisión IRREVERSIBLE. `alpha_pct` es ACUMULADO (return − benchmark desde inception), así que la serie es un *path* de niveles, no observaciones diarias independientes. `_alpha_signal_ratio` computaba media/stdev sobre los niveles → order-insensitive y engañado por el modo de suerte DOMINANTE: un caballo que salta a +7% en UN día y luego solo trackea SPY (0.2→7.0, 7.1, 6.9, 7.05) tiene media/stdev de niveles alto (el lead se ve estable) y leía `edge candidate`, pese a no mostrar edge repetible tras el día 1. Fix: el ratio ahora se computa sobre las **diferencias diarias** (active return día a día); un outlier de un solo día queda correctamente ahogado en su propio ruido. Estrictamente más conservador (solo puede volver un veredicto MÁS cauto, nunca promover de más) — alineado con "riesgo y disciplina por encima de todo". Verificado en prod: los 3 caballos siguen `inconclusive` (el gate de ruido solo entra pasados 5 alpha-días; hoy hay 2). Commit en `feature/paper-horse-race`, 1 test killer nuevo (one-lucky-day-then-coast) + 2 reescritos a semántica de incrementos, 125/125 verde.
   - [x] **Guard de frescura del checkpoint (2026-06-18, loop):** misma clase test-verde/prod-roto en el componente que alimenta la decisión IRREVERSIBLE. `checkpoint_report` leía solo la curva persistida y NO tenía noción de "hoy": si el cron L–V se cae (Mac dormido, LaunchAgent roto), la curva se congela pero el readout imprimía el último snapshot como si fuera actual — el operador podría hacer el go/no-go de dinero sobre data rancia. El `gap_days` solo cazaba huecos *internos*, no una curva cuyo último día quedó atrás. Añadido `_staleness_note`: cuenta días de trading (L–V, fines de semana ignorados) entre el último snapshot y `as_of` (hoy, inyectable para test) y marca `⚠ STALE` si hay ≥2 días de atraso (1 día = la corrida de hoy pendiente o el finde, no se alarma). Verificado en prod: la curva del 06-18 lee fresca (sin STALE). Descriptivo, no automatiza nada. Commit en `feature/paper-horse-race`, 5 tests nuevos, 130/130 verde.
   - [x] **Veredicto consciente de huecos en la ventana de alpha (2026-06-18, loop):** misma clase test-verde/prod-roto en el componente que alimenta la decisión IRREVERSIBLE. El gate señal-vs-ruido (`_alpha_signal_ratio`) diferencia alphas consecutivos como si fueran días de trading contiguos. Si la ventana de alpha tiene un hueco —una corrida del cron saltada, o un null intermedio por un fallo transitorio del fetch de SPY— dos observaciones realmente separadas por varios días se colapsan en UN solo incremento "diario", enmascarando justo el salto de suerte de un solo día que el gate existe para cazar. El `gap_days` de `risk_metrics` avisaba al humano con `⚠ GAP`, pero `classify_edge` consumía la serie distorsionada en silencio y aún podía emitir `edge candidate` — el aviso humano y el veredicto-máquina discrepaban. Fix: `_alpha_window_has_gap` cuenta días hábiles faltantes entre observaciones de alpha NO-null (finde ignorado, prefijo null ignorado); si la ventana no es contigua, `classify_edge` devuelve `edge? but alpha curve has gaps` en vez de promover a candidate. Estrictamente conservador (solo bloquea promoción, nunca promueve). Verificado en prod: curva contigua → sin cambio, los 3 siguen `inconclusive`. Descriptivo, no automatiza nada. Commit en `feature/paper-horse-race`, 10 tests nuevos, 140/140 verde.
   - [x] **Ancla de benchmark visible en el readout (2026-06-18, loop):** misma clase readout-engaña en el componente que alimenta la decisión IRREVERSIBLE. `alpha% = return − SPY buy&hold`, pero `checkpoint_report` mostraba la columna `alpha%` SIN el retorno de SPY contra el que se mide — `benchmark_pct` se computaba y se cargaba en cada fila pero nunca se imprimía (sí salía en el output del cron, no en la herramienta on-demand del go/no-go). El mismo +14.62% de alpha significa cosas distintas en un mercado plano que en un crash; sin el ancla el operador no puede saber qué régimen le ganaron los caballos. Añadido `_benchmark_anchor_note`: imprime `benchmark SPY buy&hold since inception: +X%` usando el `benchmark_pct` del último snapshot. Verificado en prod: SPY +1.20% → confirma que el +14.62% de momentum_rotation se ganó en un mercado casi plano, no en un crash. Descriptivo, no automatiza nada. Commit en `feature/paper-horse-race`, 2 tests nuevos, 142/142 verde.
   - [x] **Staleness calibrado al lag de barra real (2026-06-20, loop):** misma clase test-verde/prod-roto en el componente que alimenta la decisión IRREVERSIBLE. El guard `_staleness_note` (umbral 2 días hábiles) asumía snapshot_date ≈ día de la corrida, pero en prod `snapshot_day = df.index[-1].date()` = última barra *completada* = día hábil ANTERIOR a la corrida (a las 13:00 la barra de hoy no cerró; la corrida del 06-19 ancló snapshot 06-18). Ese lag estructural de 1 día significa que una curva perfectamente sana, leída en una MAÑANA de día hábil antes de la corrida de las 13:00, ya está a 2 días hábiles atrás → con umbral 2 gritaba `⚠ STALE "el cron pudo haberse detenido"` cada mañana estando sano, entrenando al operador a ignorar el único guard sobre el go/no-go de dinero. Los tests pasaban porque modelaban la mañana sana con snapshot a 1 día (cadena sin lag). Fix: `STALE_TRADING_DAYS = 3` (una curva sana oscila entre 1 día —tras la corrida— y 2 —mañana pendiente—; ≥3 = corrida genuinamente saltada, cazada el 2º día sin corrida). Test killer nuevo `test_format_no_stale_warning_on_healthy_weekday_morning` con la cadena real de prod (RED con umbral 2 → GREEN con 3). Verificado en prod: curva 06-18 leída sábado 06-20 = fresca, sin STALE. Estrictamente conservador en falsos positivos, detección de cron muerto solo 1 día más tarde (irrelevante en ventana de 2 semanas). Descriptivo, no automatiza nada. Commit en `feature/paper-horse-race`, 1 test nuevo + 1 comentario de test corregido, 144/144 verde.
   - [x] **Veredicto alineado al gate de muestra del contrato de dinero (2026-06-20, loop):** la grieta más grande del componente que alimenta la decisión IRREVERSIBLE, pero al nivel de SEMÁNTICA del veredicto vs el contrato firmado. `classify_edge` podía emitir `edge candidate` —la única cadena que lee como señal real de "ir a mirar"— con apenas **5 alpha-días** (`EDGE_MIN_DAYS`), pero `docs/ROADMAP-real-money.md` GATE #1 (acordado con Luis el 06-13) es explícito: la evidencia de edge necesita **~3–6 MESES de paper en vivo**, y "2 semanas ganando = suerte". Las 5-días eran la barra de "deja de decir inconclusive" (suficiente para leer los gates de calidad), NO la barra de edge probado — el tool usaba lenguaje edge-grade sobre evidencia plumbing-grade, y un lector que esperó la "ventana de 2 semanas" del PLAN podía confundir un `edge candidate` limpio de muestra corta con que el gate está cerca. Fix: nueva constante `EDGE_SAMPLE_MIN_DAYS = 63` (~3 meses, piso del rango del contrato) + tier nuevo `PROMISING_VERDICT` ("promising — plumbing-grade (need ~3–6 mo for edge)"); `classify_edge` solo emite `edge candidate` cuando además de pasar todos los gates de calidad la muestra ≥ `EDGE_SAMPLE_MIN_DAYS`, si no degrada a `promising`. Threaded por `build_checkpoint`/`format_checkpoint`; nota de footer `ℹ sample bar` que explica el porqué citando GATE #1. Estrictamente conservador (solo puede DEGRADAR un veredicto, nunca promover) — alineado con "riesgo y disciplina por encima de todo". Verificado en prod: sin cambio (los 3 siguen `inconclusive` con 3 alpha-días, debajo del gate de 5); el fix muerde ~06-24, cuando un caballo habría leído `edge candidate` con ~5 días y ahora leerá `promising`. Descriptivo, no automatiza nada. Commit en `feature/paper-horse-race`, 7 tests nuevos (+4 existentes ajustados con `sample_min_days` explícito), 151/151 verde.
   - [x] **Detección de huecos consciente de feriados de mercado (2026-06-22, loop):** misma clase test-verde/prod-roto en el componente que alimenta la decisión IRREVERSIBLE. La corrida del 06-22 disparó un `⚠ GAP` FALSO: `gap_days`/`_alpha_window_has_gap`/`_trading_days_after` contaban L–V como días de trading, pero **06-19 fue Juneteenth (NYSE cerrado)** → la curva 06-18→06-22 es contigua en días hábiles reales y el detector la leyó como corrida saltada. Misma trampa cry-wolf que el falso STALE arreglado el 06-20: entrena al operador a ignorar el único guard del go/no-go de dinero, y `_alpha_window_has_gap` podría bloquear un veredicto real por un feriado mal leído. Descartado `USFederalHolidayCalendar` de pandas (cuenta Columbus/Veterans Day —NYSE abre— y omite Good Friday —NYSE cierra— → *under-warn* en un guard, peor que el bug). Fix: nuevo módulo puro `live/market_calendar.py` con set NYSE explícito y fechado (2026) + `is_trading_day()`; los tres contadores ahora lo usan (single source of truth, DRY). Verificado en prod: el `⚠ GAP` desapareció, los 3 caballos siguen `inconclusive`. Estrictamente conservador (solo elimina falsos positivos; un año no listado degrada a conteo weekday-only, nunca a un trade malo). Descriptivo, no automatiza nada. Commit en `feature/paper-horse-race`, test killer de regresión (`test_market_holiday_is_not_a_gap`) + 3 fixtures movidos a ventana sin feriados + self-check del módulo, 152/152 verde.
   - [x] **PR #5 MERGEADO a prod 2026-06-23 (corrección 06-28: el PLAN lo describía como "espera a Luis", pero el código manda — ya está en `feature/paper-horse-race`):** el snapshot diario se fechaba con `df.index[-1]` (`run_trading_system.py:176`), que a las 13:00 CST (mercado abierto) incluía la **barra parcial intradía de hoy**. La corrida del 06-22 grabó snapshot `2026-06-22` sobre un cierre intradía → fila 06-22 (momentum return +20.30% / alpha +19.19%) reflejaba media sesión, no cierre. **Fix `drop_in_progress_bars(snapshot, today)` en `live/yfinance_bars.py`** recorta cualquier barra trailing fechada hoy o después antes de que orchestrator/marks/benchmark la vean → solo data de cierre asentado. Función pura, 4 tests de regresión. **Merge commit `04da5f4` (2026-06-23 19:13), y el reset de 4 caballos del 06-25 (be08ffb/0eb2dc6) se construyó ENCIMA → la fuente recorta barras en curso en cada corrida del field nuevo.** La fila 06-22 vieja quedó tainted pero pertenece a la curva 3-caballos archivada (`.bak-20260625`), irrelevante para el field reseteado. Los guards reader-side (`_intraday_snapshot_note`, `_settled_alpha`) siguen como defensa en profundidad.
   - [x] **Guard reader-side de barra intradía en el veredicto (2026-06-23, loop):** la corrida del 06-22 PROBÓ que el snapshot intradía es taint real, no hipótesis: momentum_rotation grabó alpha +19.19% el 06-22 (barra de media sesión) y colapsó a +11.60% el 06-23 al llegar un cierre asentado (la vol saltó a 4.51%, el max_dd a −7.42% por ese pico falso). PR #5 arregla la FUENTE (recorta barras en curso antes de grabarlas) pero espera a Luis; mientras, `checkpoint_report` —la herramienta que el operador lee para el go/no-go IRREVERSIBLE— mostraba la fila intradía SIN ningún aviso. Añadido `_intraday_snapshot_note`: como la cadencia sana SIEMPRE deja el último snapshot ≥1 día hábil atrás (a las 13:00 la barra de hoy no cerró), un snapshot fechado HOY es la firma inequívoca de la barra parcial → marca `⚠ INTRADAY` avisando que return/alpha/vol de la última fila son provisionales y no deben pesar en ninguna decisión hasta que un cierre asentado la reemplace. Independiente de PR #5 (guard del lector, no de la fuente). Verificado en prod: la fila 06-23 dispara el aviso. Estrictamente descriptivo, no automatiza ni muta nada. Commit en `feature/paper-horse-race`, 2 tests nuevos (killer same-day + no-fire en curva sana con lag), 159/159 verde.
   - [x] **Veredicto sobre cierres asentados — barra intradía fuera del gate de alpha-días (2026-06-23, loop):** misma clase reader-warns/machine-consumes en el componente que alimenta la decisión IRREVERSIBLE, y la grieta más oportuna: HOY fue el primer veredicto no-inconclusive y su cruce de αdays=5 se apoyaba en la barra intradía sin asentar del 06-23. El aviso `⚠ INTRADAY` advertía al humano que return/alpha/vol de la última fila son provisionales ("06-22 +19.19% → +11.60% al cierre"), pero `build_checkpoint` seguía metiendo ese alpha provisional en `classify_edge` Y contándolo en `alpha_days`. Fix: `_settled_alpha(dated, as_of)` suelta cualquier snapshot fechado HOY (la firma intradía, igual que `_intraday_snapshot_note` usa `last==as_of`) antes de computar serie/alpha_days/gap; la fila de display y el aviso siguen mostrando el parcial, solo el veredicto lee cierres asentados. Estrictamente conservador (solo BAJA el conteo, nunca promueve). Independiente de PR #5 (fuente, espera a Luis). Verificado en prod: los 3 caballos caen de no-inconclusive a `inconclusive` (αdays 5→4); el 5º alpha-día asentado real cae sobre el cierre del 06-23. Commit `a99714a`, 2 tests killer, 161/161 verde.
   - [ ] **Acumular alpha del NUEVO field de 4 caballos. Ventana nueva arranca ~2026-06-26** (inception=06-25; el día 1 tiene alpha null por construcción — la barra asentada 06-24 es anterior a inception, el guard `resolve_benchmark` lo logueó correctamente). **Estado al 2026-06-25 (día 1, RESET):** corrida 13:00 exit 0, 504/504 símbolos; los 4 caballos flat a $25k/0.00% (momentum 14 pos, rsi_mr 10, donchian 10, confirmed_mr 5). Curva con UN snapshot (06-24) × 4 caballos, alpha null. Re-empieza la cuenta de alpha-días desde 0 → primer veredicto no-inconclusive cae ~5 días hábiles después de que el alpha arranque (≈06-26 + 5 ≈ 07-03). Toda la batería de guards de `checkpoint_report` (alpha-days, gate de ruido, STALE/GAP/INTRADAY, selection-bias, sample-bar) aplica igual al field de 4. Leer con `checkpoint_report`: si ningún caballo es alpha-positivo y estable, NO hay edge.
     - **Footgun de día 1 (2026-06-25, observado):** `SATS` (EchoStar) está en el universo estático S&P 500 (503) y yfinance le da precio, pero Alpaca devuelve `asset "SATS" not found` → la orden falla grácil (exit 0) y el caballo que lo eligió pierde ese slot a cash, sesgando su retorno vs los demás. Clase universo-vs-ejecución (un nombre opera en backtest/selección pero no en Alpaca). **FLAG para Luis, NO auto-fix** — el arreglo (pre-filtrar el universo contra los assets tradeables de Alpaca) cambia qué se opera en la carrera recién reseteada; va como branch+PR, no silent-ship. Drag pequeño (1 slot de 1 caballo) pero persiste si SATS sigue en el universo. **→ PR #6 abierto (2026-06-29, loop, rama `fix/exclude-untradeable-sats`):** excluye `SATS` vía `UNTRADEABLE_AT_BROKER` en `sp500_constituents()` (fuente única, symbols+sector_map consistentes), 3 tests nuevos, 177/177 verde. NO mergeado — cambia qué opera la carrera viva → decisión de Luis. ponytail: set fijado, upgrade path = `get_all_assets()` si crece.
     - **Audit 2026-06-30 13:00 (loop):** corrida aterrizó sana (Mac despierto, el loop lo mantuvo arriba) — exit 0, 504/504 símbolos, benchmark SPY=True, `state.json` re-escrito 13:00:24. Snapshot **fechado 06-29** (no 06-30) por diseño: a las 13:00 CST la barra diaria de hoy aún no cierra, así que cada corrida registra la **sesión completa previa** — verificado que NO duplica (curva = 06-24/06-26/06-29, 4 entradas/fecha exactas). Primer alpha real del field de 4 ya en curva. `checkpoint_report`: los 4 caballos **inconclusive (2/5 αdays)** — donchian_breakout líder (+1.62% α), rsi_mr peor (-4.92% α); confirmed_mr -2.68%, momentum +0.20%. GAP de 1 día = el hueco permanente del reset 06-25 (esperado). Veredicto no-inconclusive sigue proyectado ~07-03. Nada que shippear este ciclo: sistema verde, solo falta acumular días.
   - [x] (Mejora) Persistir snapshot diario de equity/alpha por caballo para una curva, no solo el último corte. Hecho 2026-06-14: `live/equity_snapshot.py` (JSONL append-only, idempotente por día) cableado en `run_trading_system.py` → `data/ledgers/equity_curve.jsonl`. 68/68 tests verde. Empieza a acumular en la próxima corrida L-V.

2. **[HECHO — esperando data] Métrica de riesgo, no solo retorno.** Un caballo que gana con drawdown brutal no es edge.
   - [x] `live/risk_metrics.py`: max-drawdown (peak-to-trough) + volatilidad (stdev de retornos diarios) por caballo, leyendo `equity_curve.jsonl`. Cableado en `run_trading_system.py` → imprime tabla de riesgo tras el snapshot. Hecho 2026-06-15, 77/77 tests verde (9 nuevos en `test_risk_metrics.py`).
   - [ ] Los números solo son útiles con ~varios días de curva acumulada (empieza a llenarse en la próxima corrida L-V). Leer en el checkpoint junto a alpha.

3. **[PENDIENTE — decisión operador] Checkpoint de 2 semanas.** Al cierre de la ventana: leer attribution + alpha y decidir con Luis. Solo si hay alpha real y consistente → recién evaluar $250 vivo con tope + kill-switch (ver `docs/ROADMAP-real-money.md`). NO antes. Esta decisión es de Luis (dinero/irreversible).

4. **[⚠ HUECO DETECTADO 2026-06-27, loop] Vigilancia operativa.** Confirmar en `data/cron.log` que las corridas L-V se ejecutan sin error tras 13:00 CST.
   - [x] **Cierre 2026-07-01 21:00 (loop):** sin cambio vs el readout de la corrida 13:00 (la curva no se toca hasta la próxima corrida L-V). `checkpoint_report` reconfirmado: 4 caballos `inconclusive (3/5 αdays)`, **momentum lidera** (α +1.26%, ret +2.95%, max_dd 0.00%), donchian 2º (α +0.15%), confirmed_mr (α −3.92%), rsi_mr peor (α −5.09%) vs SPY +1.70%. `⚠ GAP` (1) = hueco permanente del reset 06-25 (esperado), sin STALE/INTRADAY. Repo limpio. Sin acción de código (ponytail: solo faltan α-días — nada que shippear sin contaminar la ventana; PR #6 SATS y el `pmset` operativo/sudo siguen esperando a Luis, ya flaggeados, no re-spameo). **Veredicto no-inconclusive proyectado ~07-03** (07-03 fecha 07-02 = α-día 5). Lo que sigue: standup 07-02 09:00 → corrida 13:00 (α-día 4) → cierre 21:00.
   - [x] **Corrida 2026-07-01 13:00 aterrizó SANA (loop):** el Mac estaba despierto (el loop lo mantuvo arriba) → exit 0, 504/504 símbolos, benchmark SPY=True, snapshot **fechado 06-30** = **α-día 3/5** como estaba proyectado. `checkpoint_report`: los 4 caballos siguen `inconclusive (3/5 αdays)` — **momentum_rotation retomó el liderato** (α +1.26%, ret +2.95%, max_dd 0.00%, vol 0.75%), donchian 2º (α +0.15%, ret +1.85%), confirmed_mr (α −3.92%, max_dd −2.22%), rsi_mr peor (α −5.09%, max_dd −4.01%) vs SPY +1.70%. `⚠ GAP` (1) sigue siendo el hueco permanente del reset 06-25 (esperado). Sin STALE nuevo. Sin acción de código (ponytail: nada que shippear — solo faltan α-días; PR #6 SATS y el `pmset` operativo siguen esperando a Luis, ya flaggeados). **Veredicto no-inconclusive proyectado ~07-03** (la corrida 07-03 fecha 07-02 = α-día 5).
   - [x] **Standup 2026-07-01 09:00 (loop, antes de la corrida de 13:00):** estado sano y esperado. Curva fresca hasta **06-29** (la barra asentada de la corrida 06-30), sin STALE/GAP nuevos; `⚠ GAP` (1) sigue siendo el hueco permanente del reset 06-25. `checkpoint_report`: los 4 caballos `inconclusive (2/5 αdays)` — donchian líder (α +1.62%, ret +2.54%, max_dd 0.00%, vol 0.99%), momentum (α +0.20%), confirmed_mr (α −2.68%, max_dd −1.77%), rsi_mr peor (α −4.92%, max_dd −4.01%). **174/174 tests verde**, repo limpio. PR #6 (SATS) sigue esperando a Luis. `pmset -g sched` sigue **VACÍO** → la corrida de HOY 07-01 13:00 depende de que el Mac esté despierto (el loop lo mantuvo arriba las últimas 2 corridas → 06-29 y 06-30 aterrizaron sanas). Sin acción de código (ponytail: nada que shippear — la máquina de veredictos ya sintetiza riesgo, solo falta acumular α-días; fix de pmset es operativo/sudo, es de Luis, ya flaggeado, no re-spameo). **La corrida de HOY 07-01 debería fechar snapshot 06-30 = α-día 3/5; veredicto no-inconclusive proyectado ~07-03** (07-03 fecha 07-02 = α-día 5).
   - [x] **Cierre 2026-06-30 21:00 (loop):** el flag matutino de riesgo se CERRÓ — la corrida 13:00 SÍ corrió (Mac despierto, exit 0, snapshot **06-29**, benchmark SPY +0.91%). `checkpoint_report`: los 4 caballos `inconclusive (2/5 αdays)`, donchian líder (α +1.62%), rsi_mr peor (α −4.92%); momentum +0.20%, confirmed_mr −2.68%. `⚠ GAP` (1) sigue siendo el hueco permanente del reset 06-25 (esperado). Repo limpio, PR #6 aún esperando a Luis, `pmset` sigue VACÍO → riesgo persiste para mañana 07-01 si el Mac duerme. Sin acción de código (ponytail: fix pendiente es operativo `sudo pmset repeat wakeorpoweron MTWRF 12:58:00`, es de Luis — ya flaggeado, no re-spameo). Veredicto no-inconclusive proyectado ~07-03.
   - [x] Re-auditado el 2026-06-30 (loop, martes 9:00 AM — antes de la corrida de 13:00): estado sano y esperado. Última corrida 06-29 13:00 exit 0 (snapshot 06-26, la barra asentada del viernes), benchmark(SPY)=True. `checkpoint_report` lee curva fresca (sin STALE — 2 días hábiles de atraso < umbral 3, correcto en mañana pre-corrida), los 4 caballos `inconclusive` con 1 αday. `⚠ GAP` (true positive) sigue marcando el hueco **permanente del 06-25** (la corrida 06-26 nunca corrió, Mac dormido; irreversible). **`pmset -g sched` SIGUE VACÍO** → la corrida de HOY 06-30 13:00 está en el mismo riesgo de saltarse si el Mac duerme. 177/177 tests verde, repo limpio, PR #6 (SATS) abierto esperando a Luis. Sin acción de código (ponytail: el fix pendiente es operativo —`sudo pmset repeat wakeorpoweron MTWRF 12:58:00`— y es de Luis; ya flaggeado urgente el 06-29, no re-spameo hasta tener señal de la corrida de hoy). SATS sigue fallando grácil (footgun conocido, item 1).
   - [x] Re-auditado el 2026-06-29 (loop, lunes **13:01 PM — la corrida de 13:00 SÍ se ejecutó**, contradiciendo el flag STALE de la mañana): el Mac estaba despierto a las 13:00, la corrida corrió exit 0, 504/504 símbolos, benchmark(SPY)=True. **PRIMER alpha-día real del field de 4 caballos registrado** (snapshot 06-26, la barra asentada): donchian_breakout +2.69%, momentum_rotation +1.43%, confirmed_mr +0.46%, rsi_mr −1.70% vs SPY −0.72%. Los 4 leen `inconclusive (need ≥5 alpha days)` con 1 αday — correcto, recién arranca la ventana. **Daño confirmado del viernes saltado:** `⚠ GAP` (true positive) — la corrida de 06-26 nunca corrió (Mac dormido), así que el cierre asentado del **06-25 quedó PERMANENTEMENTE sin snapshot** (la corrida 06-25 fechó 06-24; hoy 06-29 fechó 06-26; nadie fechó 06-25, y yfinance no re-fecha hacia atrás). Hueco de 1 día en la curva de los 4 caballos — no catastrófico (el guard avisa, vol/max_dd con cautela) pero irreversible. **pmset sigue VACÍO** → el riesgo persiste para futuros días con Mac dormido. **FLAG sigue VIVO para Luis:** `sudo pmset repeat wakeorpoweron MTWRF 12:58:00` (sudo, su ejecución). Sin acción de código (ponytail: el fix es operativo; todos los guards —STALE matutino, GAP— dispararon correctamente). SATS sigue fallando grácil (footgun conocido, item 1). Tests no tocados.
   - [~] Auditado el 2026-06-29 (loop, lunes 9:00 AM — antes de la corrida de 13:00; **superado por la corrida exitosa de 13:00, ver bullet de arriba**): el `_staleness_note` por fin **dispara `⚠ STALE`** como se predijo: último snapshot 06-24, 3 días hábiles de atraso (06-25, 06-26, 06-29) = umbral 3. Guard funciona correctamente (true positive — el viernes 06-26 SÍ se saltó). Confirmé la causa raíz con evidencia dura: `pmset -g sched` está **VACÍO** (sin wake agendado) y `launchctl print` da `runs=0` → el LaunchAgent está cargado y bien configurado (Weekday 1-5 @ 13:00) pero launchd NO despierta el Mac, así que cualquier corrida con el Mac dormido a las 13:00 se pierde sin catch-up. **La corrida de HOY 06-29 13:00 está en el MISMO riesgo que la del viernes.** Sin acción de código (ponytail: el fix es operativo). **FLAG URGENTE a Luis:** `sudo pmset repeat wakeorpoweron MTWRF 12:58:00` — 1 comando, despierta el Mac 2 min antes de cada corrida L-V; sin esto la ventana de alpha del field de 4 caballos se acumula a tropezones (ya perdió 06-26). Requiere sudo → ejecución de Luis.
   - [⚠] Re-auditado el 2026-06-28 (loop, domingo): sin cambio vs ayer — `cron.log`/`equity_curve.jsonl` siguen congelados en 2026-06-25 13:00 (snapshot 06-24), correcto para finde (no hay corrida sábado/domingo). El hueco de Friday 06-26 (Mac dormido) **persiste sin remediar** porque el `pmset repeat wakeorpoweron` sigue pendiente de Luis (sudo). **Próxima corrida = lunes 06-29 13:00 CST.** Si el Mac está dormido otra vez, se salta y el guard `_staleness_note` por fin dispara `⚠ STALE` (06-24 → 3 días hábiles de atraso = umbral). Sin acción de código (ponytail: el fix es operativo, no de código; no re-flaggeo a Luis hasta tener señal del lunes).
   - [⚠] Re-auditado el 2026-06-27 (loop, sábado): **la corrida de Friday 06-26 NO se ejecutó.** `cron.log`, `state.json` y `equity_curve.jsonl` congelados todos en 2026-06-25 13:00; última cabecera del log = 06-25. El LaunchAgent `com.luisfer.horserace` sigue cargado (launchctl exit 0) y bien configurado (Weekday 1-5 @ 13:00), así que NO es misconfiguración: la corrida se saltó, casi seguro por **Mac dormido a las 13:00 del viernes** (launchd no hace catch-up de StartCalendarInterval si la máquina sigue dormida; `pmset` confirma que el system-sleep por idle está permitido). Impacto: el field reseteado de 4 caballos pierde el alpha-día del 06-26 → la ventana nueva acumula aún más lento. El guard `_staleness_note` NO dispara todavía (snapshot 06-24 → 2 días hábiles de atraso < umbral 3; cazará el lunes 06-29 si tampoco corre). **FLAG para Luis (cambio de scheduler de sistema → NO auto-ship):** para que la carrera no dependa de que el Mac esté despierto, agendar un `sudo pmset repeat wakeorpoweron MTWRF 12:58:00` (despierta el Mac 2 min antes de la corrida L-V). Requiere sudo y es un cambio de scheduler del host → decisión/ejecución de Luis. NO toqué el LaunchAgent ni disparé una corrida manual (una corrida off-schedule en sábado encolaría órdenes paper para el lunes open = cambio de comportamiento de la carrera). 174/174 tests verde, sin acción de código (ponytail: el fix correcto es operativo, no de código).
   - [x] Re-auditado el 2026-06-22 (loop): la corrida de las 13:00 corrió exit 0, 504/504 símbolos, benchmark(SPY)=True. **06-19 NO tuvo corrida — fue Juneteenth (NYSE cerrado), correcto, no es hueco.** Curva 06-15,16,17,18,22 (06-19 feriado). Descubierto y arreglado un falso `⚠ GAP` por feriado en el checkpoint (ver item 1) + flaggeado el snapshot sobre barra parcial intradía para Luis. 152/152 tests verde.
   - [x] Auditado el 2026-06-17: 14 corridas registradas (2026-06-02→06-17), **todas exit 0**, fechas L-V contiguas sin huecos (fines de semana saltados correctamente). `equity_curve.jsonl` con 06-15/16/17 × 3 caballos, contiguo. Benchmark SPY presente (`benchmark(SPY)=True`) en la última corrida. Sin acción de código requerida; re-auditar en cada checkpoint.
   - [x] Re-auditado el 2026-06-19 (loop): 16 corridas en el log, **todas exit 0**, contiguas L-V hasta 06-19 (la corrida de hoy 13:00 ancló snapshot 06-18, benchmark(SPY)=True, 504/504 símbolos). Curva con 06-15→06-18 × 3 caballos, contigua. 142/142 tests verde. Sin huecos, sin acción de código.
   - [x] Re-auditado el 2026-06-21 (loop, sábado→domingo sin corrida): 17 corridas en el log, **todas exit 0**, L-V hasta 06-19. El 06-18 yfinance marcó MSI "possibly delisted" → 503/504 usable, benchmark(SPY)=True, exit 0 (degradación grácil, sin caída); el 06-19 volvió a 504/504, así que fue un hiccup transitorio de yfinance, no un cambio de membresía S&P. LaunchAgent `com.luisfer.horserace` cargado en launchctl. Curva contigua 06-15→06-18, sin STALE/GAP. 151/151 tests verde. Sin acción de código. Ventana de alpha sigue madurando: 3 alpha-días, 5º (primer veredicto no-inconclusive) cae ~06-24.
   - [x] Re-auditado el 2026-06-25 (loop): **primer día del field reseteado de 4 caballos.** Corrida 13:00 CST exit 0, 504/504 símbolos. Los 4 caballos flat a $25k/0.00% (momentum 14 pos, rsi_mr 10, donchian 10, confirmed_mr 5). 2 observaciones: (a) `benchmark SPY could not be computed → alpha null this run` — correcto, inception=06-25 y la barra asentada es 06-24 (anterior a inception); el guard `resolve_benchmark` lo logueó como WARNING (no es regresión, es día 1 por construcción); arranca el 06-26. (b) `Market order failed for SATS: asset "SATS" not found` — Alpaca no tradea SATS aunque está en el universo estático; flaggeado para Luis (ver item 1, sub-bullet footgun). Tests 174/174 verde. Sin acción de código (PLAN actualizado al reset + footgun flaggeado).
   - [x] Re-auditado el 2026-06-24 (loop): corrida 13:00 CST exit 0, 504/504 símbolos, benchmark(SPY)=True. Curva contigua 06-15,16,17,18,22,23 (6 días, sin STALE/GAP/INTRADAY). Veredicto al cierre de los 5 alpha-días: **NINGÚN edge** — momentum_rotation `edge? but within noise` (alpha +11.60%, su swing diario lo ahoga), rsi_mr `edge? but unstable` (+0.60%), confirmed_mr `no edge` (−8.88%) vs SPY −0.23%. Los guards funcionan: nadie llega a candidate/promising. **Hallazgo nuevo (evidencia DURA pro-PR #5):** confirmé que el sanado de una fila intradía-tainted es NO-determinista — depende del flaky de yfinance publicando o no la barra del día en curso. Mecanismo: `append_snapshot` sobrescribe por fecha, así que una fila solo se sana si una corrida POSTERIOR se re-fecha al MISMO día. Hoy la corrida 06-24 se fechó 06-23 (yfinance no dio barra 06-24) → sobrescribió y **sanó** la fila 06-23 (27843/+11.37% intradía → 27949/+11.8% asentado). Pero la fila 06-22 (+20.30% intradía) quedó **tainted para siempre** porque la corrida del 06-23 se fechó 06-23, nunca re-tocó 06-22. Es decir: que una fila tainted se cure o no es lotería de yfinance. PR #5 lo elimina en la FUENTE (recorta la barra en curso antes de grabar) — sigue siendo decisión de Luis (toca lógica de trading). Sin acción de código este ciclo (ponytail: no guard #16; el fix correcto es PR #5).

5. **[DIFERIDO post-edge] Deuda técnica conocida:**
   - `check_time_exits` no cableado (necesita reconciliación de ledger por estrategia — mismo invariante CORE que SELL).
   - Revival de `momentum_news` (overlay en `live/news_overlay.py`) requiere fuente de noticias per-ticker o pagada; free-tier AlphaVantage no sirve.
   - Cripto, refresco manual del snapshot S&P 500 cuando cambie membresía.

## NO romper (guardrails)
- Solo corre si `ALPACA_BASE_URL=paper-api.alpaca.markets`. Sizing contra cash (no equity). Vender por qty específica, NUNCA `close_position`. NUNCA tocar secretos en cuarentena.

## Pendiente del operador (fuera del loop del bot)
- Rotar secretos en cuarentena (`~/.trading-secrets-quarantine-20260531/`): Binance, KuCoin, Telegram token, instancias EC2. Ver `CLAWBOT-BACKLOG.md`.

## 🔧 Herramientas externas a evaluar (inyectado por Luis 2026-06-22)
> Scouting de Luis (carrusel SYNTAIX AI). Exploratorio — NO derailar la ventana de paper en curso. **Cambio de arquitectura → spike en rama + PR para Luis, NUNCA auto-ship.**

- [~] **TradingAgents** (`github.com/TauricResearch/TradingAgents`, arXiv 2412.20138) — `gate portado, falta cablear (PR)`. Repo clonado en `~/dev/_tradingagents-spike` (deps instaladas, `TradingAgentsGraph` importa; correrlo end-to-end pide key LLM/Finnhub). **Patrón robado y portado:** `live/risk_gate.py` — debate multi-perspectiva (conservador/neutral/agresivo) que veta encima de `evaluate_entry`, determinista y testeable (`tests/test_risk_gate.py`, 5/5 verde). NO cableado al orchestrator todavía: enchufar después de `evaluate_entry` y antes de `executor.buy` es el PR (cambio de arquitectura → para Luis). Robar la arquitectura: analistas (fundamental/sentiment/news/technical) → **Risk Manager que vetea ANTES de ejecutar** → executor. Hoy el bot decide y ejecuta sin gate de riesgo separado; portar ese Risk Manager como gate previo a toda orden alinea con el norte ("riesgo y disciplina por encima de todo"). NO migrar a su stack; portar el patrón. Substrato natural si se materializa: **LangGraph** (máquina de estados resiliente > debate). Spike acotado: leer el paper + diseñar el gate como módulo puro testeable, PR para Luis. NO tocar el horse-race en curso hasta cerrar su ventana de alpha.

---

## 🧰 Herramientas OSS candidatas (carruseles IG · 30-jun-2026)
Fuente completa: `Sistema Luisfer/_sistema/tools-oss-candidatas.md`.

**Honestidad:** ninguno de los 6 carruseles trae una tool específica de trading. Solo aplican transversales:
- **Metabase** — dashboards de resultados del paper horse-race (sobre `results.csv` / la DB).
- **Coolify** — self-host del loop si algún día sale de tu Mac (mata rate-limits de cloud).

No forzar adopción aquí; el norte sigue siendo *edge real en paper*.
