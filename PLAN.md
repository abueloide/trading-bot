# PLAN — Paper horse-race → edge probado

> Norte: encontrar un **edge real en paper** (estrategia que le gane al mercado, no solo a las otras), con riesgo y disciplina por encima de todo. NUNCA dinero real sin edge probado. Rama: `feature/paper-horse-race` (NO mergeada).
> Estado vivo: 3 caballos × $25k sobre 1 cuenta Alpaca paper, corriendo L-V 13:00 CST vía LaunchAgent `com.luisfer.horserace`. Epoch limpio desde **2026-06-05**.

## Prioridad

1. **[EN CURSO] Medir edge contra benchmark, no solo entre caballos.**
   - [x] Benchmark SPY buy&hold + columna `alpha%` en el reporte (`live/benchmark.py`, anclado al 2026-06-05). Hecho 2026-06-14, 63/63 tests verde.
   - [x] **BUG CRÍTICO arreglado 2026-06-15:** el alpha NUNCA se calculaba en prod. SPY no es constituyente del S&P 500 → no estaba en el universo descargado → `compute_benchmark` devolvía `None` en cada corrida → `alpha_pct: null` en toda la curva y la columna `alpha%` se caía del reporte. Los tests pasaban porque inyectaban SPY en el fixture (test-verde/prod-roto). Fix: `FETCH_SYMBOLS = UNIVERSE + [SPY]` en `run_trading_system.py` (SPY se descarga pero el orchestrator solo pide símbolos de estrategia, así que NUNCA se opera). Regresión cerrada con `tests/test_benchmark_wiring.py` (5 tests). Verificado end-to-end: SPY +2.35% desde 2026-06-05. **El alpha real empieza a registrarse en la próxima corrida (mar 2026-06-16).**
   - [x] Lectura de checkpoint on-demand: `live/checkpoint_report.py` (+ `tests/test_checkpoint_report.py`, 13 tests) consolida por caballo equity/return/alpha + max-DD/vol + un veredicto descriptivo en UNA tabla, leyendo solo la curva persistida (sin red, sin Alpaca). Correr: `.venv-bt/bin/python -m live.checkpoint_report`. NO automatiza ninguna decisión.
   - [x] **Verificación end-to-end del fix de alpha (2026-06-15, loop):** confirmado que el path de prod produce benchmark SPY +2.34% (no null) → el alpha real arranca bien en la corrida del 2026-06-16. De paso encontré y arreglé un footgun latente: `get_bars_batch` descartaba en silencio un fetch de UN solo símbolo (yfinance devuelve columnas MultiIndex aun para 1 ticker; el branch `else raw` se las pasaba crudas a `_normalize` y volvían vacías). Misma clase test-verde/prod-roto que el bug de SPY-fuera-del-universo. Arreglado + 2 tests de regresión. Prod (503 símbolos) nunca lo pegó, pero un refresh benchmark-only de SPY sí. 97/97 verde. Commit `dcc6259`.
   - [x] **Guard anti-regresión silenciosa (2026-06-15, loop):** el bug de alpha-null vivió 10 días sin un solo aviso en el log. Extraído `resolve_benchmark()` en `run_trading_system.py` que loguea un `WARNING` a `cron.log` cuando el benchmark sale None, distinguiendo "SPY nunca se fetcheó" de "se fetcheó pero sin barra at/after inception". Ahora la clase de bug se caza en cada corrida, no al cerrar la ventana de 2 semanas. Commit `02343d3`, 4 tests nuevos (`tests/test_resolve_benchmark.py`), 101/101 verde. (El snapshot del 2026-06-15 sigue en null porque el fix `13afb6e` aterrizó 13:06, después de la corrida de 13:00 — el alpha real arranca el 2026-06-16.)
   - [x] **Detección de huecos en la curva (2026-06-16, loop):** misma clase test-verde/prod-roto. `vol`/`max_dd` asumen que dos snapshots consecutivos son días de trading consecutivos; si una corrida del cron se salta un día (justo lo que vigila el item #4), la curva queda con hueco y la volatilidad se infla en silencio + una curva rala podría pasar el veredicto `≥5 días`. `risk_metrics` ahora cuenta `gap_days` (días hábiles L-V faltantes, fines de semana ignorados) por caballo; `checkpoint_report` imprime un aviso `⚠ GAP` ruidoso cuando algún caballo tiene curva no contigua. Descriptivo, no automatiza nada. Commit en `feature/paper-horse-race`, 9 tests nuevos, 110/110 verde.
   - [x] **Veredicto honesto: alpha-días reales + estabilidad, no equity-días (2026-06-16, loop):** misma clase test-verde/prod-roto, pero en el componente que alimenta la decisión IRREVERSIBLE de dinero (`classify_edge` en `checkpoint_report`). El veredicto (a) gateaba en `n_days` = días de equity, pero el alpha fue null hasta 06-16 → la curva llega a 5 equity-días antes que a 5 alpha-días reales, y habría declarado "edge candidate" con 1-2 observaciones de alpha mientras mostraba "5 days"; y (b) leía solo el alpha del ÚLTIMO snapshot, no estabilidad — un rebote negativo→positivo el último día pasaba como edge, violando el "positivo **y estable**" del PLAN. Fix: `classify_edge` ahora recibe la **serie** de alpha cronológica, gatea en observaciones reales (no-null) y marca `edge? but unstable (alpha dipped ≤0)` si el alpha tocó ≤0 dentro de la ventana. Nueva nota `ℹ alpha lag` en el readout explica por qué la columna "days" puede verse larga y el veredicto seguir inconclusive. Verificado en prod: los 3 caballos leen `inconclusive (need ≥5 alpha days)` con 2 alpha-días reales. Descriptivo, no automatiza nada. Commit en `feature/paper-horse-race`, 9 tests reescritos/nuevos, 115/115 verde.
   - [x] **HITO confirmado en prod (2026-06-16, loop):** la corrida de las 13:00 CST registró por PRIMERA VEZ alpha real (no-null) en `equity_curve.jsonl`: momentum_rotation +9.20%, rsi_mr +2.29%, confirmed_mr −4.10% vs SPY +1.90%. Cierra los 10 días de bug alpha-null en producción; el log muestra `benchmark(SPY)=True`, exit 0. La ventana útil de alpha corre ahora ~2026-06-16 → 2026-06-30.
   - [x] **Ventana de riesgo divulgada (2026-06-16, loop):** misma clase test-verde/prod-roto, en el componente que alimenta la decisión IRREVERSIBLE. `return%`/`equity` son acumulados desde inception (2026-06-05), pero `max_dd%`/`vol%` solo cubren la curva persistida, que empezó a acumular el 2026-06-15 (~10 días tarde — `equity_snapshot.py` se cableó el 06-14). Un lector del checkpoint leería `max_dd −2.09%` como "peor DD desde inception" y se perdería cualquier drawdown anterior al primer snapshot. `checkpoint_report` ahora adjunta `curve_start`/`curve_end` por caballo e imprime una nota `ℹ risk window` que dice explícitamente que max_dd es worst-since-curve-start, NO worst-since-inception. Descriptivo, no automatiza nada. Commit `52d75c9`, 3 tests nuevos, 118/118 verde.
   - [x] **Columna alpha-days en el readout (2026-06-16, loop):** misma clase readout-engaña en el componente que alimenta la decisión IRREVERSIBLE. La tabla mostraba `days` = largo de la curva de equity, pero el veredicto gatea en **alpha-days** (observaciones de alpha no-null). Mientras el alpha fue null pre-benchmark divergen: prod lee `days=2` / `alpha-days=1`, pero la tabla solo mostraba 2 junto a `need ≥5 alpha days` — un lector no podía ver que solo tenemos 1 alpha-día. La nota `ℹ alpha lag` que lo explicaría sigue suprimida hasta que los equity-días solos lleguen a 5, así que la columna es la única señal honesta en el caso cotidiano. Añadida columna `αdays`. Descriptivo, no automatiza nada. Commit `7ce815a`, 1 test nuevo, 119/119 verde.
   - [x] **Gate señal-vs-ruido en el veredicto (2026-06-17, loop):** el componente que alimenta la decisión IRREVERSIBLE declaraba "edge candidate" con alpha positivo y estable, pero SIN noción de magnitud-vs-ruido. Con 3 caballos × ~10 días, que uno le gane a SPY por suerte es EL riesgo estadístico central: un alpha de +0.1% saltarín leía idéntico a uno de +9% sólido. `classify_edge` ahora computa un info-ratio crudo (media del alpha / su stdev diaria); si la media no supera su propia dispersión (`ratio < 1`) marca `edge? but within noise (alpha < its own swing)` en vez de candidate. Stdev 0 (alpha plano positivo) o <2 obs → no penaliza (un alpha consistente y plano es MÁS convincente, no menos). Disciplina sobre retorno, descriptivo, no automatiza nada. Commit en `feature/paper-horse-race`, 2 tests nuevos, 121/121 verde.
   - [ ] Dejar acumular alpha hasta cerrar la ventana de 2 semanas. OJO: la curva de alpha real arranca el 2026-06-16 (el bug la tuvo en null hasta hoy), así que la ventana útil de alpha corre ~2026-06-16 → 2026-06-30. Leer con `checkpoint_report`: si ningún caballo es alpha-positivo y estable, NO hay edge.
   - [x] (Mejora) Persistir snapshot diario de equity/alpha por caballo para una curva, no solo el último corte. Hecho 2026-06-14: `live/equity_snapshot.py` (JSONL append-only, idempotente por día) cableado en `run_trading_system.py` → `data/ledgers/equity_curve.jsonl`. 68/68 tests verde. Empieza a acumular en la próxima corrida L-V.

2. **[HECHO — esperando data] Métrica de riesgo, no solo retorno.** Un caballo que gana con drawdown brutal no es edge.
   - [x] `live/risk_metrics.py`: max-drawdown (peak-to-trough) + volatilidad (stdev de retornos diarios) por caballo, leyendo `equity_curve.jsonl`. Cableado en `run_trading_system.py` → imprime tabla de riesgo tras el snapshot. Hecho 2026-06-15, 77/77 tests verde (9 nuevos en `test_risk_metrics.py`).
   - [ ] Los números solo son útiles con ~varios días de curva acumulada (empieza a llenarse en la próxima corrida L-V). Leer en el checkpoint junto a alpha.

3. **[PENDIENTE — decisión operador] Checkpoint de 2 semanas.** Al cierre de la ventana: leer attribution + alpha y decidir con Luis. Solo si hay alpha real y consistente → recién evaluar $250 vivo con tope + kill-switch (ver `docs/ROADMAP-real-money.md`). NO antes. Esta decisión es de Luis (dinero/irreversible).

4. **[VERIFICADO 2026-06-17, loop] Vigilancia operativa.** Confirmar en `data/cron.log` que las corridas L-V se ejecutan sin error tras 13:00 CST.
   - [x] Auditado el 2026-06-17: 14 corridas registradas (2026-06-02→06-17), **todas exit 0**, fechas L-V contiguas sin huecos (fines de semana saltados correctamente). `equity_curve.jsonl` con 06-15/16/17 × 3 caballos, contiguo. Benchmark SPY presente (`benchmark(SPY)=True`) en la última corrida. Sin acción de código requerida; re-auditar en cada checkpoint.

5. **[DIFERIDO post-edge] Deuda técnica conocida:**
   - `check_time_exits` no cableado (necesita reconciliación de ledger por estrategia — mismo invariante CORE que SELL).
   - Revival de `momentum_news` (overlay en `live/news_overlay.py`) requiere fuente de noticias per-ticker o pagada; free-tier AlphaVantage no sirve.
   - Cripto, refresco manual del snapshot S&P 500 cuando cambie membresía.

## NO romper (guardrails)
- Solo corre si `ALPACA_BASE_URL=paper-api.alpaca.markets`. Sizing contra cash (no equity). Vender por qty específica, NUNCA `close_position`. NUNCA tocar secretos en cuarentena.

## Pendiente del operador (fuera del loop del bot)
- Rotar secretos en cuarentena (`~/.trading-secrets-quarantine-20260531/`): Binance, KuCoin, Telegram token, instancias EC2. Ver `CLAWBOT-BACKLOG.md`.
