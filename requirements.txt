# Core trading + data
alpaca-py>=0.30.0
yfinance>=0.2.36
pandas>=2.2.0
numpy>=1.26.0

# Telegram monitoring
python-telegram-bot>=21.0

# Config / scheduling / IO
python-dotenv>=1.0.0
loguru>=0.7.0
httpx>=0.27.0
apscheduler>=3.10.0

# Phase 4 — alternative data (optional)
# Install only if you have a Quiver subscription.
# quiverquant>=1.0

# Phase 5 — news intelligence (optional)
# FinBERT requires ~2GB RAM; on smaller VPS use Alpha Vantage instead.
# transformers>=4.40.0
# torch>=2.2.0
# anthropic>=0.25.0

# Optional for testing strategies offline
pandas-ta>=0.3.14b
